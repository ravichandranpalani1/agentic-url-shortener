Build a URL shortener service from scratch with a create API, a redirect API, per-code click analytics, custom aliases, link expiration, rate limiting, and a health/metrics endpoint. It must handle at least 3 concurrent redirects without errors and must recover its state after a restart.