server:
  port: ${PORT:8080}

app:
  data-dir: ${DATA_DIR:./data}
  rate-limit:
    capacity: ${RATE_LIMIT_CAPACITY:20}
    refill-per-second: ${RATE_LIMIT_REFILL_PER_SEC:5.0}

spring:
  application:
    name: agentic-url-shortener

logging:
  level:
    root: INFO
